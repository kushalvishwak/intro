# intro

This repository now includes a generic Python API client skeleton at `api_client.py`.

Usage:

```bash
export API_BASE_URL="https://example.com/api"
export API_KEY="your_api_key"
python3 api_client.py
```

Edit `api_client.py` to set the exact endpoint paths and request payloads for your target API.
